# ===== 아래 설정은 모두 프론트엔드 설정 페이지에서 입력 가능합니다 =====
# .env 파일 없이도 서버 실행 후 http://localhost:3001/settings 에서 설정할 수 있습니다.

# POP3 Configuration (선택 - 프론트엔드에서 설정 가능)
# POP3_HOST=mail.example.com
# POP3_PORT=995
# POP3_USER=your-email@example.com
# POP3_PASSWORD=your-password
# POP3_TLS=true

# AI Provider (선택 - 프론트엔드에서 설정 가능)
# AI_PROVIDER=claude
# ANTHROPIC_API_KEY=sk-ant-xxxxx
# CLAUDE_MODEL=claude-haiku-4-5-20251001
# OLLAMA_HOST=http://localhost:11434
# OLLAMA_MODEL=gemma3:12b

# Notion (선택 - 프론트엔드에서 설정 가능)
# NOTION_API_KEY=ntn_xxxxx
# NOTION_DATABASE_ID=your-database-id

# Encryption key (선택 - 미설정 시 자동 생성되어 data/encryption.key에 저장)
# ENCRYPTION_KEY=your-64-char-hex-key

# Server
PORT=3001
NODE_ENV=production

# Frontend URL (개발 모드 CORS용, 프로덕션에서는 불필요)
# FRONTEND_URL=http://localhost:5173
